[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

# Standalone workspace — prevents cargo from treating this as a member
# of the parent tar-rs workspace.
[workspace]

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
xshell = "0.2"
